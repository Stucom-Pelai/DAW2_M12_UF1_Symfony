{# templates/worker/index.html.twig #}

{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create Worker</h1>

    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.surname) }}
    {{ form_row(form.birthdate) }}
    {{ form_end(form) }}
{% endblock %}
